pipeline {
    agent any

    environment {
        SONAR_TOKEN_CREDENTIAL_ID = 'sonar-token' // Replace with your credential ID
        SONAR_INSTALLATION_NAME = 'SonarServer' // Replace with your SonarQube installation name
    }

    stages {
        

         stage('Build Angular App') {
            steps {
                sh '''
                    #!/bin/bash
                    npm install
                    ng build --prod
                '''
            }
        }




    }
}
